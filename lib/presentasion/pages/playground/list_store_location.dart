import 'package:flutter/material.dart';

class ListStoreLocation extends StatelessWidget {
  const ListStoreLocation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('List Sample'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
